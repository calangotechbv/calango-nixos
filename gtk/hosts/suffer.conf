# suffer: the laptop.
#
# Sourced last by gtk/appearance.conf, so anything set here wins. Same variable
# names as that file: theme, icons, cursor, cursor_size, font, mono_font.
#
# Empty until this machine wants something different. Example:
#   cursor_size="32"
